module Vreg

import Types

defn read(ks: kstate, rid: u64) : u64 =
  ks.vregs[rid]

defn write(ks: kstate, rid: u64, v: u64) : kstate =
  let vregs = ks.vregs in
  let vregs = vregs[rid] <- v in
  ks.vregs <- vregs