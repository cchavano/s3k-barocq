module Sched
module Sched

type kstate = Kernel::state
type kstate = Kernel::state

defn delete(ks: kstate, from: u64, to: u64) : kstate =
  let mask = 0xffffUL in
  (* MISSING CODE *)
  ks
  ks

defn update(ks: kstate, pid: u64, end: u64, from: u64, to: u64) : kstate =
  let mask = 0xffffUL in
  (* MISSING CODE *)
  ks
  ks