module Trap

import Types

defn handler(ks: kstate, mcause : u64, mtval: u64) : kstate =
  if mcause == 8UL then
    Syscall::handler(ks, ks.next_pid)
  else if (mcause as i64) < 0L then
    Interrupt::handler(ks, ks.next_pid)
  else
    Exception::handler(ks, ks.next_pid, mcause, mtval)
