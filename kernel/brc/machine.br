module Machine

import Types

decl rtc_time_get : (@write machine) -> machine

@static
defn time_sync(ks: kstate) : kstate =
  ks.mc <- rtc_time_get(ks.mc)

decl time_read : (@read machine) -> u64

decl timeout_read : (@read machine, u64) -> u64

decl csrr_mhartid : (@read machine) -> u64
